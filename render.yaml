services:
  - type: web
    name: rapiddispatch-live-ops-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run server
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 24
      - key: CLIENT_ORIGIN
        sync: false
